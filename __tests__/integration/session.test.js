const request = require('supertest');

const app = require('../../src/app');
const { User } = require('../../src/app/models');

describe('', () => {
    it('should', () => {

        expect().toBe();
    });
});